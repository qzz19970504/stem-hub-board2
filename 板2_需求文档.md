# 板2需求文档

## UART

三路串口均为 115200、8N1、无硬件流控。

- 主串口 UART1：PA9=TX、PA10=RX，负责接收和解析上位机 AT 指令。
- UART2：PA2=TX、PA3=RX，负责与外部设备透传。注意 PA2/PA3 的方向由 STM32F103 固定复用功能决定。
- UART3：PB10=TX、PB11=RX，负责与外部设备透传。

## LED 与 PWM

- LED2：PA8，推挽输出，高电平点亮。FreeRTOS systemTask 启动后常亮。
- PWM_LED：PB9/TIM4_CH4，高电平有效，频率 25 kHz，占空比可设为 0%～100%。
- PWM 使用 Gamma 2.2 映射和非阻塞渐变；渐变时间默认 500 ms，可在 0～10000 ms 设置并掉电保存。
- 呼吸灯测试在 0%～100% 间循环；测试期间普通 PWM 命令无效，但关闭 18V 始终立即生效。

## NMOS

- NMOS1：PB4。
- NMOS2：PB15。
- NMOS3：PB6。

三路 NMOS 均为高电平导通。上电初始化和软件启动时必须先输出低电平，保证默认关闭。

## Buck 电源控制

- 18V：PB3，低电平开启、高电平关闭。
- 12V：PB12，低电平开启、高电平关闭。

上电默认关闭两路 Buck。12V 关闭时禁止开启 NMOS；18V 关闭时禁止设置非零 PWM。

## AT 与透传

完整指令、错误响应及硬件验收方法见 [docs/board2-at-uart-pwm.md](docs/board2-at-uart-pwm.md)。
